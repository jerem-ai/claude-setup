# GitHub Setup (for Jeremy)

Run these commands after creating the repo at github.com/jeremyshank/ceo-claude-setup:

```bash
cd ~/Desktop/ceo-claude-setup
git remote add origin https://github.com/jeremyshank/ceo-claude-setup.git
git push -u origin main
git push --tags
```

Then share the install command with your CEO:
```bash
claude plugin install github:jeremyshank/ceo-claude-setup
```
